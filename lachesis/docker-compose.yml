version: '3'
services:
        node1:
                ports:
                        - "8000:8000"
                environment:
                        - DATADIR=./build/node1
                build: .
        node2:
                environment:
                        - DATADIR=./build/node2
                build: .
        node3:
                environment:
                        - DATADIR=./build/node3
                build: .


